# 当前工作树里保留哪些 method submodule。
#
# 根仓库 main 分支始终记录全部 method 的 gitlink，本文件只决定本机工作树里
# 实际 checkout 哪些。切换：./lab method focus <profile>
#
# 名称取自 scripts/lab.py 的 _METHODS 注册表；"*" 表示全部。

default: all

profiles:
  all: ["*"]
